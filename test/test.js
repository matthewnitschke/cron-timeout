const { expect } = require('chai')
const { cronTimeout, cronInterval } = require('../src/cron-timeout.js')


describe('Tests', () => {
    it('should test something', () => {
        expect(true).to.be.equal(true)
    })
})